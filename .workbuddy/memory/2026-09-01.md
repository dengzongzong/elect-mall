# 2026-09-01 工作日志

- 澄清了 elect-mall 的部署真相：无 CI/CD 自动触发，实际为「本地构建 → scp 上传 → systemd 重启」。服务器（腾讯云 CentOS 7，/root/elect-mall）由 systemd 守护 `elect-mall-backend`（php -S 127.0.0.1:8000 跑 adapter.php，库 root/123456）与 `elect-mall-frontend`（零依赖 Node deploy/prod-server.cjs，8080/8081，代理 /api、/admin 到后端）。
- 新增 `deploy/deploy-remote.sh`：按真实服务器写的可用部署脚本（构建两前端 → 打包最小集 → scp → 覆盖 /root/elect-mall → restart frontend，改 adapter.php 时加 --backend）。语法检查通过、已加可执行权限。
- 校正 `DEPLOY.md`：顶部加醒目提示，说明其与真实架构（Nginx+php-fpm+GitHub Actions 模板）不符，指引用 deploy-remote.sh。
- 提交并推送：`172027a`（deploy-remote.sh + DEPLOY.md 修正）。
- 备注：本地 `origin/master` 跟踪引用在本沙箱 git 中显示异常（fetch/push 报已更新但 rev-parse 仍是旧值），以 `git ls-remote` 远程真实状态为准即可，换机重新 clone 即正常。

- 后台「商品管理」列表新增「一级分类 / 二级分类」两列。根因：后端 `product/page` 原本只 LEFT JOIN 一次 category 拿到单个 `category_name`，未区分一级/二级。改法：所有 `SELECT ... c.name as category_name FROM product p LEFT JOIN category c` 处统一加 `LEFT JOIN category cParent ON c.parent_id=cParent.id` 并返回 `c.name as l2_name, cParent.name as l1_name`（含含 brand 的多商品查询）；前端 `admin-web/src/views/Product.vue` 在料号列后加两列（空值显示 `-`）。已构建 admin-web、scp `adapter.php`+`admin-web/dist`、restart backend+frontend 上线；接口与 join 逻辑均验证通过（L2 商品能正确带出一级）。注意：现有 5 个种子商品都挂在一级分类(id=1)下，故一级列暂空；要让一级列有值，需把商品挂到二级分类（编辑表单选 L2 即可），尚未改动种子数据。
- 提交并推送：`51cd2b2`。

- 用户指出"商品管理"里的一级/二级分类列与"分类管理"层级对不上：根因是 5 个种子商品只挂在一级大类(id=1)，导致二级列显示一级名。修法：① 把可对应品牌的 4 个种子商品改挂二级子类（P1/P2→muRata 村田 id2、P3→Yageo 国巨 id10、P4→TDK id3；P5 松下无对应子类保留一级）；② 后端 `product/page` 一级大类筛选改为递归包含二级子类（`p.category_id = ? OR p.category_id IN (SELECT id FROM category WHERE parent_id = ?)`，后台397行/商城928行两处），避免商城按一级浏览时丢商品；③ 前端列标签改为"一级大类/二级子类"与分类管理一致。`adapter.php` 上传时一度被沙箱 502 中断、仅 adapter 落地而 dist 未传，后重建 admin-web 重传 dist 并 restart，已核验服务端 dist 含新标签、admin 200、商城一级浏览递归包含 5 商品。提交推送：`97f2d61`。

- 品牌专区按 oneyac.com/brand/list.html 参考重构（mall-web `Brand.vue` + 后端 `brand/list`）。改动：① 后端 `brand/list` 增加实时 `product_count`（子查询 `COUNT(product WHERE brand_id=b.id AND deleted=0)`），前端卡片显示"X 件商品"/"敬请期待"；② 前端加顶部红色渐变 banner（授权代理品牌列表）、搜索框（品牌名+描述模糊）、分类筛选下拉（扁平化全部类目 L1+L2 文本匹配）、卡片"授权代理品牌"橙标签（is_cooperate=1）；③ 字母索引优化（无效字母置灰、选中平滑滚动）。构建 mall-web、scp dist+adapter.php、restart frontend+backend 上线。验证：brand/list 返回 product_count 与 is_cooperate 正确、首页 200、新 Brand 组件已打进 dist。注意：mall-web 是 **hash 路由**（`createWebHashHistory`），故访问路径为 `/#/brand` 而非 `/brand`（直接 curl `/brand` 404 是 hash 模式误测，非 bug）；prod-server 商城 server 的 `spaFallback:false` 也印证 hash 模式。提交推送：`5edd8d2`。

- 品牌专区端到端测试（27/27 全通过）中发现真实 bug：原前端按 `name[0]` 取首字母分组，35 个品牌里 32 个中文名被归到 `#`，字母导航实际失效。修复：后端 `brand/list` 新增 `letter` 字段（正则优先取英文括号 `()` 内首字母，否则取首个连续英文字母，无英文则 `#`），前端改用 `brand.letter` 分组；验证 TDK→T、国巨(YAGEO)→Y、基美(KEMET)→K、村田(Murata)→M、松下(Panasonic)→P 正确，A-Z 分组分布正常。重测 27/27 通过。提交推送：`56d4dd3`。

- BOM配单参考 oneyac.com/bom/copy.html 重构（mall-web `Bom.vue` + 新增 `mall-web/public/bom-template.csv`）。改动：① 录入方式改为 Tabs——主入口"粘贴物料清单"（大文本框，前端按行解析 `型号 数量`，支持空格/Tab/逗号(中英文)分隔，实时显示已识别料号数）+ "开始处理"按钮；次入口"上传BOM文件"（含模板下载 `/bom-template.csv`）；② 加"只匹配自营商品"开关（控制结果表仅展示 matched）；③ 结果表加"商品名称"列；④ 结果区加"全部加入购物车"按钮（占位提示开发中）、历史区加"管理BOM列表"入口。后端 `bom/submit` 本就支持 `items[]`（每项 partNo+quantity）且已返回 name 字段，无需改。构建 mall-web、scp dist、restart frontend，模板可下载(200)。端到端测试 13/13 通过（注：首次用假料号致 name/金额断言失败，改用真实 part_no 后全过，系测试数据问题非 bug）。提交推送：`7455d77`。
