# 2026-09-01 工作日志

- 澄清了 elect-mall 的部署真相：无 CI/CD 自动触发，实际为「本地构建 → scp 上传 → systemd 重启」。服务器（腾讯云 CentOS 7，/root/elect-mall）由 systemd 守护 `elect-mall-backend`（php -S 127.0.0.1:8000 跑 adapter.php，库 root/123456）与 `elect-mall-frontend`（零依赖 Node deploy/prod-server.cjs，8080/8081，代理 /api、/admin 到后端）。
- 新增 `deploy/deploy-remote.sh`：按真实服务器写的可用部署脚本（构建两前端 → 打包最小集 → scp → 覆盖 /root/elect-mall → restart frontend，改 adapter.php 时加 --backend）。语法检查通过、已加可执行权限。
- 校正 `DEPLOY.md`：顶部加醒目提示，说明其与真实架构（Nginx+php-fpm+GitHub Actions 模板）不符，指引用 deploy-remote.sh。
- 提交并推送：`172027a`（deploy-remote.sh + DEPLOY.md 修正）。
- 备注：本地 `origin/master` 跟踪引用在本沙箱 git 中显示异常（fetch/push 报已更新但 rev-parse 仍是旧值），以 `git ls-remote` 远程真实状态为准即可，换机重新 clone 即正常。

- 后台「商品管理」列表新增「一级分类 / 二级分类」两列。根因：后端 `product/page` 原本只 LEFT JOIN 一次 category 拿到单个 `category_name`，未区分一级/二级。改法：所有 `SELECT ... c.name as category_name FROM product p LEFT JOIN category c` 处统一加 `LEFT JOIN category cParent ON c.parent_id=cParent.id` 并返回 `c.name as l2_name, cParent.name as l1_name`（含含 brand 的多商品查询）；前端 `admin-web/src/views/Product.vue` 在料号列后加两列（空值显示 `-`）。已构建 admin-web、scp `adapter.php`+`admin-web/dist`、restart backend+frontend 上线；接口与 join 逻辑均验证通过（L2 商品能正确带出一级）。注意：现有 5 个种子商品都挂在一级分类(id=1)下，故一级列暂空；要让一级列有值，需把商品挂到二级分类（编辑表单选 L2 即可），尚未改动种子数据。
- 提交并推送：`51cd2b2`。
